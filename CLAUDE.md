# CLAUDE.md

このファイルは、Claude Code（claude.ai/code）がこのリポジトリで作業する際のガイドラインです。

## プロジェクト概要

- リポジトリ名: strategy
- リモートリポジトリ: https://github.com/hiro0112/strategy.git
- 現時点ではプロジェクトの内容は未定義です。ファイルが追加され次第、このセクションを更新してください。

## Git運用ルール

**最重要ルール：コードを変更するたびに、必ずGitHubへプッシュすること。**

- ファイルの追加・修正・削除など、リポジトリに変更を加えたら、作業の区切りごとにコミットし、そのままGitHubの `origin`（`main` ブランチ、または作業中のブランチ）へプッシュする。
- 変更をローカルに溜め込んだまま放置しない。1つの意味のある変更単位（1機能・1修正など）が完了したら、都度コミット＆プッシュする。
- コミットメッセージは変更内容が分かるように簡潔に記述する（日本語・英語どちらでも可。プロジェクト内で統一する）。
- 基本フローは以下の通り：

  ```bash
  git add -A
  git commit -m "変更内容の要約"
  git push origin <ブランチ名>
  ```

- `main` ブランチへ直接コミットしてよいが、大きめの変更や試行錯誤を伴う作業では作業用ブランチを切り、完了後に `main` へマージ（またはPRを作成）してからプッシュすることを推奨する。
- 機密情報（APIキー、パスワード、個人情報など）は絶対にコミットしない。`.gitignore` を適切に整備すること。
- プッシュに失敗した場合（リモートが先に進んでいる等）は、`git pull --rebase` などで取り込んでから再度プッシュする。

## 開発時の注意事項

- 変更を行った後は、可能な範囲で内容を確認（動作確認・レビュー等）してからコミットする。
- 大きな変更を行う場合は、事前に方針をユーザーと確認してから着手する。
